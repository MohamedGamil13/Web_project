# Runtime
NODE_ENV=development
PORT=5050

# Database
MONGO_URI=mongodb://127.0.0.1:27017/hotel_booking

# Auth
JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=10

# CORS — origin allowed to call the API (the frontend dev server)
CLIENT_URL=http://localhost:5173
